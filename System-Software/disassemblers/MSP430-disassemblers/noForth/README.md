# noForth MSP430 disassembler
The noForth disassembler is a little shorter due to the internal words `?DUP`, `VALUE` and `PCHAR`. 
It is also protected from forgetting by using a `SHIELD` at the end of the file.
